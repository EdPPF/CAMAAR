When(/^eu clico em ver templates existentes$/) do
  pending
end

When(/^deve aparecer os templates existentes$/) do
  pending
end

When(/^deve aparecer uma mensagem dizendo que não existe templates$/) do
  pending
end